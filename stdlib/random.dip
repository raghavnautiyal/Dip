# A function to select a random value from a list

function random(list_)

    variable random_number = random_int(0, length(list_) - 1)

    return (list_ / random_number)
end

variable x = random(["yes","no","maybe","ok","never"])

print(x)