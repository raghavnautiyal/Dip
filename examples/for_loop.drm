# A dreamscript program to print out something while omitting a few values

variable num = input_integer("Till what number do you want to iterate?  ")

variable omit = input_integer("Which value do you want to omit?  ")

for i = 0 to num then
    if i == omit then
        continue
    else 
        print(i)
    end
end