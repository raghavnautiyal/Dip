# A dreamscript program to check if a triangle is valid or not

variable angle1 = print("Enter the measure of the first angle:  ") +  input_integer() 

variable angle2 =  print("Enter the measure of the second angle:  ") +  input_integer() 

variable angle3 =  print("Enter the measure of the third angle:  ") +  input_integer() 

if angle1 + angle2 + angle3 != 180 then 
 print("Sorry, not a valid triangle :(") 
else 
    print("Yay, it's a valid triangle :)")
end